#include "main.h"
#include <string.h>

/**
 *_strlen - returns length of a string
 *@s: string whose length is determined
 *Return: length of the string
 */

int _strlen(char *s)
{
	return (strlen(*s));
}
